nap_restore_SPR_effect = { if = { limit = { nap_era_country = yes is_subject = no has_war = no SPR = { exists = no } any_owned_state = { is_core_of = SPR is_controlled_by = ROOT } NOT = { any_owned_state = { is_core_of = SPR NOT = { is_controlled_by = ROOT } } } NOT = { check_variable = { nap_treasury < 12 } } } release = SPR if = { limit = { SPR = { exists = yes } } puppet = SPR add_to_variable = { nap_treasury = -12 } nap_era_clamp = yes } else = { add_political_power = 35 } } }
nap_restore_POR_effect = { if = { limit = { nap_era_country = yes is_subject = no has_war = no POR = { exists = no } any_owned_state = { is_core_of = POR is_controlled_by = ROOT } NOT = { any_owned_state = { is_core_of = POR NOT = { is_controlled_by = ROOT } } } NOT = { check_variable = { nap_treasury < 12 } } } release = POR if = { limit = { POR = { exists = yes } } puppet = POR add_to_variable = { nap_treasury = -12 } nap_era_clamp = yes } else = { add_political_power = 35 } } }
nap_restore_TUR_effect = { if = { limit = { nap_era_country = yes is_subject = no has_war = no TUR = { exists = no } any_owned_state = { is_core_of = TUR is_controlled_by = ROOT } NOT = { any_owned_state = { is_core_of = TUR NOT = { is_controlled_by = ROOT } } } NOT = { check_variable = { nap_treasury < 12 } } } release = TUR if = { limit = { TUR = { exists = yes } } puppet = TUR add_to_variable = { nap_treasury = -12 } nap_era_clamp = yes } else = { add_political_power = 35 } } }
nap_restore_SWE_effect = { if = { limit = { nap_era_country = yes is_subject = no has_war = no SWE = { exists = no } any_owned_state = { is_core_of = SWE is_controlled_by = ROOT } NOT = { any_owned_state = { is_core_of = SWE NOT = { is_controlled_by = ROOT } } } NOT = { check_variable = { nap_treasury < 12 } } } release = SWE if = { limit = { SWE = { exists = yes } } puppet = SWE add_to_variable = { nap_treasury = -12 } nap_era_clamp = yes } else = { add_political_power = 35 } } }
nap_restore_DEN_effect = { if = { limit = { nap_era_country = yes is_subject = no has_war = no DEN = { exists = no } any_owned_state = { is_core_of = DEN is_controlled_by = ROOT } NOT = { any_owned_state = { is_core_of = DEN NOT = { is_controlled_by = ROOT } } } NOT = { check_variable = { nap_treasury < 12 } } } release = DEN if = { limit = { DEN = { exists = yes } } puppet = DEN add_to_variable = { nap_treasury = -12 } nap_era_clamp = yes } else = { add_political_power = 35 } } }
nap_restore_POL_effect = { if = { limit = { nap_era_country = yes is_subject = no has_war = no POL = { exists = no } any_owned_state = { is_core_of = POL is_controlled_by = ROOT } NOT = { any_owned_state = { is_core_of = POL NOT = { is_controlled_by = ROOT } } } NOT = { check_variable = { nap_treasury < 12 } } } release = POL if = { limit = { POL = { exists = yes } } puppet = POL add_to_variable = { nap_treasury = -12 } nap_era_clamp = yes } else = { add_political_power = 35 } } }
nap_restore_NET_effect = { if = { limit = { nap_era_country = yes is_subject = no has_war = no NET = { exists = no } any_owned_state = { is_core_of = NET is_controlled_by = ROOT } NOT = { any_owned_state = { is_core_of = NET NOT = { is_controlled_by = ROOT } } } NOT = { check_variable = { nap_treasury < 12 } } } release = NET if = { limit = { NET = { exists = yes } } puppet = NET add_to_variable = { nap_treasury = -12 } nap_era_clamp = yes } else = { add_political_power = 35 } } }
nap_restore_NAP_effect = { if = { limit = { nap_era_country = yes is_subject = no has_war = no NAP = { exists = no } any_owned_state = { is_core_of = NAP is_controlled_by = ROOT } NOT = { any_owned_state = { is_core_of = NAP NOT = { is_controlled_by = ROOT } } } NOT = { check_variable = { nap_treasury < 12 } } } release = NAP if = { limit = { NAP = { exists = yes } } puppet = NAP add_to_variable = { nap_treasury = -12 } nap_era_clamp = yes } else = { add_political_power = 35 } } }
nap_restore_SAR_effect = { if = { limit = { nap_era_country = yes is_subject = no has_war = no SAR = { exists = no } any_owned_state = { is_core_of = SAR is_controlled_by = ROOT } NOT = { any_owned_state = { is_core_of = SAR NOT = { is_controlled_by = ROOT } } } NOT = { check_variable = { nap_treasury < 12 } } } release = SAR if = { limit = { SAR = { exists = yes } } puppet = SAR add_to_variable = { nap_treasury = -12 } nap_era_clamp = yes } else = { add_political_power = 35 } } }
nap_restore_PAP_effect = { if = { limit = { nap_era_country = yes is_subject = no has_war = no PAP = { exists = no } any_owned_state = { is_core_of = PAP is_controlled_by = ROOT } NOT = { any_owned_state = { is_core_of = PAP NOT = { is_controlled_by = ROOT } } } NOT = { check_variable = { nap_treasury < 12 } } } release = PAP if = { limit = { PAP = { exists = yes } } puppet = PAP add_to_variable = { nap_treasury = -12 } nap_era_clamp = yes } else = { add_political_power = 35 } } }
nap_restore_VEN_effect = { if = { limit = { nap_era_country = yes is_subject = no has_war = no VEN = { exists = no } any_owned_state = { is_core_of = VEN is_controlled_by = ROOT } NOT = { any_owned_state = { is_core_of = VEN NOT = { is_controlled_by = ROOT } } } NOT = { check_variable = { nap_treasury < 12 } } } release = VEN if = { limit = { VEN = { exists = yes } } puppet = VEN add_to_variable = { nap_treasury = -12 } nap_era_clamp = yes } else = { add_political_power = 35 } } }
nap_restore_TUS_effect = { if = { limit = { nap_era_country = yes is_subject = no has_war = no TUS = { exists = no } any_owned_state = { is_core_of = TUS is_controlled_by = ROOT } NOT = { any_owned_state = { is_core_of = TUS NOT = { is_controlled_by = ROOT } } } NOT = { check_variable = { nap_treasury < 12 } } } release = TUS if = { limit = { TUS = { exists = yes } } puppet = TUS add_to_variable = { nap_treasury = -12 } nap_era_clamp = yes } else = { add_political_power = 35 } } }
nap_restore_BAV_effect = { if = { limit = { nap_era_country = yes is_subject = no has_war = no BAV = { exists = no } any_owned_state = { is_core_of = BAV is_controlled_by = ROOT } NOT = { any_owned_state = { is_core_of = BAV NOT = { is_controlled_by = ROOT } } } NOT = { check_variable = { nap_treasury < 12 } } } release = BAV if = { limit = { BAV = { exists = yes } } puppet = BAV add_to_variable = { nap_treasury = -12 } nap_era_clamp = yes } else = { add_political_power = 35 } } }
nap_restore_SAX_effect = { if = { limit = { nap_era_country = yes is_subject = no has_war = no SAX = { exists = no } any_owned_state = { is_core_of = SAX is_controlled_by = ROOT } NOT = { any_owned_state = { is_core_of = SAX NOT = { is_controlled_by = ROOT } } } NOT = { check_variable = { nap_treasury < 12 } } } release = SAX if = { limit = { SAX = { exists = yes } } puppet = SAX add_to_variable = { nap_treasury = -12 } nap_era_clamp = yes } else = { add_political_power = 35 } } }
nap_restore_HAN_effect = { if = { limit = { nap_era_country = yes is_subject = no has_war = no HAN = { exists = no } any_owned_state = { is_core_of = HAN is_controlled_by = ROOT } NOT = { any_owned_state = { is_core_of = HAN NOT = { is_controlled_by = ROOT } } } NOT = { check_variable = { nap_treasury < 12 } } } release = HAN if = { limit = { HAN = { exists = yes } } puppet = HAN add_to_variable = { nap_treasury = -12 } nap_era_clamp = yes } else = { add_political_power = 35 } } }
nap_restore_WUR_effect = { if = { limit = { nap_era_country = yes is_subject = no has_war = no WUR = { exists = no } any_owned_state = { is_core_of = WUR is_controlled_by = ROOT } NOT = { any_owned_state = { is_core_of = WUR NOT = { is_controlled_by = ROOT } } } NOT = { check_variable = { nap_treasury < 12 } } } release = WUR if = { limit = { WUR = { exists = yes } } puppet = WUR add_to_variable = { nap_treasury = -12 } nap_era_clamp = yes } else = { add_political_power = 35 } } }
nap_client_aid_BAT_effect = { if = { limit = { tag = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury < 12 } } BAT = { exists = yes is_subject_of = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury > 88 } } } } add_to_variable = { nap_treasury = -12 } BAT = { add_to_variable = { nap_treasury = 12 } add_to_variable = { nap_legitimacy = 2 } add_to_variable = { nap_reform = 2 } nap_era_clamp = yes nap_era_refresh = yes } nap_era_clamp = yes } }
nap_client_aid_HOL_effect = { if = { limit = { tag = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury < 12 } } HOL = { exists = yes is_subject_of = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury > 88 } } } } add_to_variable = { nap_treasury = -12 } HOL = { add_to_variable = { nap_treasury = 12 } add_to_variable = { nap_legitimacy = 2 } add_to_variable = { nap_reform = 2 } nap_era_clamp = yes nap_era_refresh = yes } nap_era_clamp = yes } }
nap_client_aid_WAR_effect = { if = { limit = { tag = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury < 12 } } WAR = { exists = yes is_subject_of = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury > 88 } } } } add_to_variable = { nap_treasury = -12 } WAR = { add_to_variable = { nap_treasury = 12 } add_to_variable = { nap_legitimacy = 2 } add_to_variable = { nap_reform = 2 } nap_era_clamp = yes nap_era_refresh = yes } nap_era_clamp = yes } }
nap_client_aid_ITA_effect = { if = { limit = { tag = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury < 12 } } ITA = { exists = yes is_subject_of = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury > 88 } } } } add_to_variable = { nap_treasury = -12 } ITA = { add_to_variable = { nap_treasury = 12 } add_to_variable = { nap_legitimacy = 2 } add_to_variable = { nap_reform = 2 } nap_era_clamp = yes nap_era_refresh = yes } nap_era_clamp = yes } }
nap_client_aid_WES_effect = { if = { limit = { tag = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury < 12 } } WES = { exists = yes is_subject_of = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury > 88 } } } } add_to_variable = { nap_treasury = -12 } WES = { add_to_variable = { nap_treasury = 12 } add_to_variable = { nap_legitimacy = 2 } add_to_variable = { nap_reform = 2 } nap_era_clamp = yes nap_era_refresh = yes } nap_era_clamp = yes } }
nap_client_aid_RHC_effect = { if = { limit = { tag = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury < 12 } } RHC = { exists = yes is_subject_of = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury > 88 } } } } add_to_variable = { nap_treasury = -12 } RHC = { add_to_variable = { nap_treasury = 12 } add_to_variable = { nap_legitimacy = 2 } add_to_variable = { nap_reform = 2 } nap_era_clamp = yes nap_era_refresh = yes } nap_era_clamp = yes } }
nap_client_aid_BAV_effect = { if = { limit = { tag = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury < 12 } } BAV = { exists = yes is_subject_of = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury > 88 } } } } add_to_variable = { nap_treasury = -12 } BAV = { add_to_variable = { nap_treasury = 12 } add_to_variable = { nap_legitimacy = 2 } add_to_variable = { nap_reform = 2 } nap_era_clamp = yes nap_era_refresh = yes } nap_era_clamp = yes } }
nap_client_aid_SAX_effect = { if = { limit = { tag = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury < 12 } } SAX = { exists = yes is_subject_of = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury > 88 } } } } add_to_variable = { nap_treasury = -12 } SAX = { add_to_variable = { nap_treasury = 12 } add_to_variable = { nap_legitimacy = 2 } add_to_variable = { nap_reform = 2 } nap_era_clamp = yes nap_era_refresh = yes } nap_era_clamp = yes } }
nap_client_aid_WUR_effect = { if = { limit = { tag = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury < 12 } } WUR = { exists = yes is_subject_of = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury > 88 } } } } add_to_variable = { nap_treasury = -12 } WUR = { add_to_variable = { nap_treasury = 12 } add_to_variable = { nap_legitimacy = 2 } add_to_variable = { nap_reform = 2 } nap_era_clamp = yes nap_era_refresh = yes } nap_era_clamp = yes } }
nap_client_aid_NAP_effect = { if = { limit = { tag = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury < 12 } } NAP = { exists = yes is_subject_of = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury > 88 } } } } add_to_variable = { nap_treasury = -12 } NAP = { add_to_variable = { nap_treasury = 12 } add_to_variable = { nap_legitimacy = 2 } add_to_variable = { nap_reform = 2 } nap_era_clamp = yes nap_era_refresh = yes } nap_era_clamp = yes } }
nap_client_aid_NET_effect = { if = { limit = { tag = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury < 12 } } NET = { exists = yes is_subject_of = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury > 88 } } } } add_to_variable = { nap_treasury = -12 } NET = { add_to_variable = { nap_treasury = 12 } add_to_variable = { nap_legitimacy = 2 } add_to_variable = { nap_reform = 2 } nap_era_clamp = yes nap_era_refresh = yes } nap_era_clamp = yes } }
nap_client_aid_SAR_effect = { if = { limit = { tag = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury < 12 } } SAR = { exists = yes is_subject_of = FRA has_country_flag = nap_era_initialized NOT = { check_variable = { nap_treasury > 88 } } } } add_to_variable = { nap_treasury = -12 } SAR = { add_to_variable = { nap_treasury = 12 } add_to_variable = { nap_legitimacy = 2 } add_to_variable = { nap_reform = 2 } nap_era_clamp = yes nap_era_refresh = yes } nap_era_clamp = yes } }
